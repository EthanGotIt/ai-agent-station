<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ethan.ai.infrastructure.dao.DemoOrderMapper">

    <resultMap id="DemoOrderMap" type="cn.ethan.ai.infrastructure.dao.po.DemoOrderPO">
        <id column="order_id" property="orderId"/>
        <result column="user_id" property="userId"/>
        <result column="status" property="status"/>
        <result column="days_since_delivery" property="daysSinceDelivery"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="DemoOrderMap">
        SELECT order_id,
               user_id,
               status,
               CASE
                   WHEN delivered_at IS NULL THEN NULL
                   ELSE DATEDIFF(CURRENT_DATE, DATE(delivered_at))
               END AS days_since_delivery
        FROM demo_order
        WHERE order_id = #{orderId}
    </select>

    <update id="updateStatusForRefund">
        UPDATE demo_order
        SET status = 'REFUNDED',
            updated_at = NOW(6)
        WHERE order_id = #{orderId}
          AND user_id = #{userId}
          AND status IN ('PAID', 'PROCESSING', 'DELIVERED')
    </update>

</mapper>
