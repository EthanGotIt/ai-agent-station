<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ethan.ai.infrastructure.dao.AgentTurnMapper">

    <insert id="insert" parameterType="cn.ethan.ai.infrastructure.dao.po.AgentTurnPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_turn
            (turn_id, case_id, session_id, actor_id, turn_type, input_summary, output_summary,
             status, start_time, end_time, create_time, update_time)
        VALUES
            (#{turnId}, #{caseId}, #{sessionId}, #{actorId}, #{turnType}, #{inputSummary}, #{outputSummary},
             #{status}, #{startTime}, #{endTime}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateByTurnId" parameterType="cn.ethan.ai.infrastructure.dao.po.AgentTurnPO">
        UPDATE agent_turn
        SET output_summary = #{outputSummary},
            status = #{status},
            end_time = #{endTime},
            update_time = #{updateTime}
        WHERE turn_id = #{turnId}
    </update>

</mapper>
