# V2 交付验收报告

## 结论

V2 于 2026-08-12 在独立非生产 MySQL 与 DashScope 环境完成验收，状态为 `PASSED`。候选代码基线为 `89ebda1`；该修订包含控制台 SSE 与空成功响应的最终收口修复。

## 验收结果

| 范围 | 结果 |
|---|---|
| 完整真实验收 | 45/45 通过，0 失败 |
| 数据库 | 初始化/重置完成，Workflow 重启恢复与退款幂等通过 |
| Router / ReAct 模型 | `qwen3.7-plus`，Thinking 按现有配置启用 |
| Router / Skill 稳定性 | 近期订单比较、单订单复盘、订单加物流、售后政策、会话偏好均为 5/5 |
| ASK | 确认、拒绝、取消、超时全部通过，拒绝类路径无意外写入 |
| Tool | 五个只读 Tool 与 `save_session_preference` 的权限和用户/会话隔离通过 |
| 输出安全 | API、SSE 与日志只保留脱敏生命周期，不暴露原始 Thinking、Skill 正文、完整 Tool 结果、Prompt 或凭据 |
| 控制台人工冒烟 | 文本 SSE、业务卡片、QuestionCard 回答、连续 ASK、记忆创建/读取/删除通过 |

完整真实验收在 2026-08-12 11:23:11 至 11:34:08（Asia/Shanghai）执行，数据库重置状态为 `COMPLETED`。本地脱敏运行产物为 `target/live-acceptance/report-20260812T033408Z.md` 和同名 JSON；`target` 不作为源码发布物。

## 离线门禁

- 规范检查通过。
- Python 测试 18/18 通过。
- 严格计划审计 23/23 通过。
- Maven：core 58、infrastructure 32、app 24，共 114 项通过。
- 控制台最终测试 9/9 通过，生产构建通过。

## 验收中完成的收口

- ReAct 每回合通过 AgentScope Skill Loader 确定性加载目标 Skill，并只对外发送脱敏 Tool 生命周期。
- 会话偏好保存使用同一生产 Tool、ASK 权限与运行时隔离，连续偏好逐项确认。
- 控制台按事件类型区分结构化与文本 SSE，避免将 `route/progress/tool/done` 误报为 JSON 解析失败。
- 控制台接受删除接口的 204 空成功响应，成功后正常刷新记忆列表。

## 范围边界

本结论是项目级 V2 交付验收，不包含并发压测、故障注入、生产安全评审、Docker 部署或上线准备。API、Tool Schema、权限和确定性 Workflow 仍是强制边界；Skill 与模型输出不能覆盖这些代码约束。后续扩展另开 V2.1 任务。
